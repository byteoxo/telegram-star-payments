[project]
name = "telegram-star-payments"
version = "0.1.0"
description = "Create Telegram Stars (XTR) invoice links and listen for payment completion."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot[socks]>=21.6",
    "python-dotenv>=1.0.1",
]
